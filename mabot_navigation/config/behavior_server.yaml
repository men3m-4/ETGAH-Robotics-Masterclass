behavior_server:
  ros__parameters:
    use_sim_time: true
    enable_stamped_cmd_vel: false

    cycle_frequency: 10.0

    # Costmaps and footprints used for collision checking.
    local_costmap_topic: local_costmap/costmap_raw
    global_costmap_topic: global_costmap/costmap_raw

    local_footprint_topic: local_costmap/published_footprint
    global_footprint_topic: global_costmap/published_footprint

    # Match the rest of the MABot navigation stack.
    local_frame: odom
    global_frame: map
    robot_base_frame: base_footprint
    transform_tolerance: 0.5

    behavior_plugins:
      - "spin"
      - "backup"
      - "wait"

    spin:
      plugin: "nav2_behaviors::Spin"

    backup:
      plugin: "nav2_behaviors::BackUp"

    wait:
      plugin: "nav2_behaviors::Wait"

    # Predict recovery motion for collision checking.
    simulate_ahead_time: 2.0

    # Conservative rotational limits for MABot.
    max_rotational_vel: 0.80
    min_rotational_vel: 0.15
    rotational_acc_lim: 1.50
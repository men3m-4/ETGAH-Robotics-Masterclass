slam_toolbox:
  ros__parameters:

    # Optimization solver
    solver_plugin: solver_plugins::CeresSolver
    ceres_linear_solver: SPARSE_NORMAL_CHOLESKY
    ceres_preconditioner: SCHUR_JACOBI
    ceres_trust_strategy: LEVENBERG_MARQUARDT
    ceres_dogleg_type: TRADITIONAL_DOGLEG
    ceres_loss_function: None

    # Use the Gazebo simulation clock
    use_sim_time: true

    # MABot frames and laser scan topic
    odom_frame: odom
    map_frame: map
    base_frame: base_footprint
    scan_topic: /scan

    # Localize against a previously serialized pose graph
    mode: localization

    # The launch file will supply the full pose graph prefix.
    # Do not include .posegraph or .data in this value.
    map_file_name: ""

    # Initial guess in the saved map: x, y, yaw.
    # Correct the estimate in RViz after startup if needed.
    map_start_pose: [0.0, 0.0, 0.0]

    # Logging and scan processing
    debug_logging: false
    throttle_scans: 1
    minimum_time_interval: 0.5

    # Publish the map -> odom transform
    transform_publish_period: 0.02
    restamp_tf: false
    transform_timeout: 0.2
    tf_buffer_duration: 30.0

    # Occupancy grid settings
    map_update_interval: 1.0
    resolution: 0.05
    min_laser_range: 0.0
    max_laser_range: 20.0

    # Serialization and interactive graph editing
    stack_size_to_use: 40000000
    enable_interactive_mode: false

    # Scan matching
    use_scan_matching: true
    use_scan_barycenter: true

    # Motion thresholds: meters and radians
    minimum_travel_distance: 0.15
    minimum_travel_heading: 0.15
    check_min_dist_and_heading_precisely: false

    # Keep a small buffer of recent scans for localization
    scan_buffer_size: 3
    scan_buffer_maximum_scan_distance: 10.0

    # Matching nearby scans
    link_match_minimum_response_fine: 0.1
    link_scan_maximum_distance: 1.5

    # Matching against the saved pose graph
    do_loop_closing: true
    loop_search_maximum_distance: 3.0
    loop_match_minimum_chain_size: 3
    loop_match_maximum_variance_coarse: 3.0
    loop_match_minimum_response_coarse: 0.35
    loop_match_minimum_response_fine: 0.45

    # Local correlation search
    correlation_search_space_dimension: 0.5
    correlation_search_space_resolution: 0.01
    correlation_search_space_smear_deviation: 0.1

    # Loop closure correlation search
    loop_search_space_dimension: 8.0
    loop_search_space_resolution: 0.05
    loop_search_space_smear_deviation: 0.03

    # Scan matcher penalties and angular search
    distance_variance_penalty: 0.5
    angle_variance_penalty: 1.0
    fine_search_angle_offset: 0.00349
    coarse_search_angle_offset: 0.349
    coarse_angle_resolution: 0.0349
    minimum_angle_penalty: 0.9
    minimum_distance_penalty: 0.5
    use_response_expansion: true

    # Occupancy classification
    min_pass_through: 2
    occupancy_threshold: 0.1